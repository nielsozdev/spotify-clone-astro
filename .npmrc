# just in case a private registry is configured in ~/.npmrc
# registry = https://registry.npmjs.org/
# package-lock = false
# progress = false
# .npmrc
registry = https://registry.npmjs.org
save-exact = true
engine-strict=true